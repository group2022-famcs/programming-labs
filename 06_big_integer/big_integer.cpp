#include "big_integer.h"
#include <not_implemented.h>

BigInteger fromInt(int x) {
    NOT_IMPLEMENTED(x);
}

BigInteger fromString(const std::string& s) {
    NOT_IMPLEMENTED(s);
}

std::string toString(const BigInteger& x) {
    NOT_IMPLEMENTED(x);
}

CompareResult compare(const BigInteger& lhs, const BigInteger& rhs) {
    NOT_IMPLEMENTED(lhs, rhs);
}

BigInteger add(const BigInteger& lhs, const BigInteger& rhs) {
    NOT_IMPLEMENTED(lhs, rhs);
}

BigInteger subtract(const BigInteger& lhs, const BigInteger& rhs) {
    NOT_IMPLEMENTED(lhs, rhs);
}

BigInteger multiply(const BigInteger& lhs, const BigInteger& rhs) {
    NOT_IMPLEMENTED(lhs, rhs);
}
